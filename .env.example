# ---------------------------------
# API Keys - DO NOT COMMIT ACTUAL KEYS
# ---------------------------------

# Get your key from Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY="your_gemini_api_key_here"

# Get your key from Stability AI: https://platform.stability.ai/account/keys
STABILITY_API_KEY="your_stability_api_key_here"

# Firebase Service Account File Path
# 1. Go to Firebase Console > Project Settings > Service accounts
# 2. Click "Generate new private key" and download the file.
# 3. Place the downloaded JSON file in the root directory of the project (e.g., alongside the .env file).
# 4. Set the variable below to the name of that file.
GOOGLE_APPLICATION_CREDENTIALS="your-service-account-file.json"

# Firebase Storage Bucket URL
# 1. Go to your Firebase Console.
# 2. Navigate to the "Storage" section.
# 3. The URL is shown at the top of the Files tab (e.g., gs://your-bucket-name.appspot.com).
# 4. Copy the part *without* "gs://".
APP_STORAGE_BUCKET="your-bucket-name.appspot.com"


# ---------------------------------
# Generation Settings (Optional)
# ---------------------------------

# Default number of cards to generate if not specified in the UI
NUM_CARDS_TO_GENERATE=10

# Image generation provider: 'stability' or 'vertex'
IMAGE_PROVIDER=stability

# Delay in milliseconds between image generation requests to avoid rate limiting
IMAGE_DELAY_MS=1000


# ---------------------------------
# Model Configuration (Optional)
# ---------------------------------

# Gemini model for text generation
GEMINI_TEXT_MODEL=gemini-1.5-flash-latest

# Stability AI model for image generation
STABILITY_MODEL=stable-diffusion-xl-1024-v1-0


# ---------------------------------
# Stripe Payment Processing (Credit System)
# ---------------------------------

# Stripe API Keys - Get from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY="sk_test_your_stripe_secret_key_here"
STRIPE_PUBLISHABLE_KEY="pk_test_your_stripe_publishable_key_here"

# Stripe Webhook Secret - Get from https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET="whsec_your_stripe_webhook_secret_here"

# Firebase Admin Configuration for Credit System
FIREBASE_PROJECT_ID="your_firebase_project_id"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_firebase_private_key_here\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-your_project@your_project.iam.gserviceaccount.com"

# Admin User IDs (comma-separated Firebase UIDs for admin access)
ADMIN_USER_IDS="admin_uid_1,admin_uid_2"

# Database Configuration
DATABASE_URL="./fresquia.db"
NODE_ENV="development"


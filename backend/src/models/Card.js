class Card {
  constructor(id, title, description, icon, category) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.icon = icon;
    this.category = category;
  }
}

module.exports = Card;
